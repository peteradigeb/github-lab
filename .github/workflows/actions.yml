#!/bin/bash



name: Hello Workflow

on: push

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run:
          echo "Hello, Peter 🚀"
          date
          uname -a
